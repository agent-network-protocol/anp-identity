# ANP Identity demos

- [DSH plugin creation, approval and signed HTTP](./dsh-http/README.md): an ordinary plugin requests an identity, waits for user confirmation, and uses an approved identity to call a local HTTPS verifier. Includes source, isolated setup helpers and real native-signature regression tests.

Demos are development examples, not production deployment templates. Never commit or share their ignored `runtime/` directories.
